export interface Stats {
    gender: string;
    age: number | '';
    height: number | '';
    weight: number | '';
    activity_level: string;
  }
  
export interface Goals {
    objective: string;
    target_weight: number | '';
    target_date: string;
  }